---
id: R1
name: notation_first
trigger:
  draft_section: [method, approach]
  task: draft
---

**规则**：Method section 开头定义 notation。

**要求**：第一段或第一个 subsection 定义所有核心符号，引用 `math_commands.tex`。读者不应该在读公式时遇到未定义的符号。

**模板**：
> "Let $\mathcal{X} \in \mathbb{R}^{d}$ denote the input space, $p_\theta$ the learned distribution…"
