\caption{%
  \textbf{Neural scaling law: model size vs.\ validation loss.}
  Each point represents one model ($n{=}80$); axes are $\log_{10}$ of parameter count and
  validation loss respectively.
  The red line is an ordinary least-squares fit
  ($y = {-0.0449}\,x + 0.621$, $R^{2}{=}0.877$),
  closely matching the Chinchilla-style expected slope of $-0.045$.
  The highlighted orange point (\textbf{OursModel}) is the data point nearest to
  $(\log_{10} N{=}9.45,\;\mathcal{L}{=}0.210)$.
}
