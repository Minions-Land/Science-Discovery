\caption{%
  \textbf{Four-panel overview of the proposed method.}
  \textbf{(A)} Three-stage pipeline: input encoding, attention-and-memory, and skill-gated decoding, connected by a residual skip connection.
  \textbf{(B)} Ablation study across five configurations; the full model achieves \textbf{64.7}, while removing the attention module causes the largest drop (to 58.2).
  \textbf{(C)} Training loss curves over 30 steps; our method converges faster and reaches a final loss \textbf{0.19 lower} than the baseline.
  \textbf{(D)} Sensitivity sweep over a $5\times5$ grid of hyperparameters S0--S4 and P0--P4; cell values are scores (0--1); darker blue indicates higher performance.
  All values are single-run; no error bars shown.%
}
