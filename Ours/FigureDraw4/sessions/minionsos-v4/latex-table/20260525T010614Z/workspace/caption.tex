\caption{Comparison of 4 methods on 5 reasoning benchmarks (all metrics in \%, higher is better). \textbf{OursModel achieves the best result on every benchmark}, with a lead of up to $5.3$ pp over the strongest baseline (Method-B) on GPQA. \textbf{Bold} entries are the best per column; values whose mean $+$ std overlaps the best mean $-$ std are also bolded (within-1$\sigma$ rule). Mean $\pm$ std over $5$ seeds.}
