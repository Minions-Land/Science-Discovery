\caption{%
  \textbf{10-class confusion matrix.}
  Each row represents the true class; each column represents the predicted class.
  Cell colour encodes the row-normalised fraction of samples (Blues colormap, 0--1);
  cell text shows the raw count.
  \textbf{Diagonal entries (correct predictions) range from 69 (C1) to 92 (C8)},
  annotated in white bold; off-diagonal misclassifications are annotated in grey.
  $N = 100$ samples per class (1{,}000 total); counts are single-run predictions
  with no aggregation across seeds.
}
