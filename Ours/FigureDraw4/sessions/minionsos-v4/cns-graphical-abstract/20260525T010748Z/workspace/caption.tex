\caption{RetroDiff retrieves top-$k$ ChEMBL exemplars via ECFP4 fingerprints and feeds them as conditioning context to a diffusion decoder, yielding \textbf{2$\times$ the hit rate of baseline diffusion at fixed compute}, a +18\% Vina docking-score gain, 60\%-faster convergence, and a confirmed scaling law ($R^{2}>0.9$ across five model sizes). All quantitative claims are derived from \texttt{claim.json}; molecule-icon schematics and training curves are illustrative.}
