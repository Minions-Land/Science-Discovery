\caption{%
  \textbf{ROC and Precision-Recall curves for four binary classifiers.}
  \emph{Left:} Receiver Operating Characteristic (ROC) curves; the dashed diagonal
  marks chance-level performance.
  \emph{Right:} Precision-Recall (PR) curves; the dashed horizontal line indicates
  the positive-class prevalence ($\approx0.40$).
  AUROC and Average Precision (AP) values are reported in each legend entry.
  OursModel achieves the highest AUROC and AP among all methods evaluated.
  Curves are computed directly from the raw classifier scores provided in
  \texttt{data.json}.%
}
