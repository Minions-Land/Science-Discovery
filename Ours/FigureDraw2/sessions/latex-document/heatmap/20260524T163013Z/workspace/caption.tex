\caption{%
  10-class confusion matrix (counts). Rows correspond to true classes (C1--C10)
  and columns to predicted classes. Diagonal cells (correct predictions, shown
  with a \textit{YlGn} colormap) are annotated in bold white; off-diagonal
  errors are shaded with a sequential \textit{Blues} colormap scaled to their
  own range. Per-class accuracy ranges from 69\% (C1) to 92\% (C8).
}
