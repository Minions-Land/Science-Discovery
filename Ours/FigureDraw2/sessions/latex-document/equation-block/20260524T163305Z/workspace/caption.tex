\caption{RetroDiff training-objective derivation. Eq.~\eqref{eq:elbo}: variational lower bound (ELBO) obtained via Jensen's inequality. Eq.~\eqref{eq:decomp}: canonical decomposition into a reconstruction term and per-step KL divergences. Eq.~\eqref{eq:retro}: RetroDiff's retrieval-augmented objective, conditioning each sample's ELBO on a set $R(x)$ of $k$ retrieved exemplars.}
