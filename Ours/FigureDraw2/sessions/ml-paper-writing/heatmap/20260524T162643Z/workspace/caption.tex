\caption{Confusion matrix for a 10-class classifier (counts over a held-out test set). Rows indicate true class; columns indicate predicted class. Diagonal cells (correct predictions, shaded in YlOrRd) are annotated with their counts; off-diagonal cells (misclassifications, shaded in Blues) show raw error counts. Per-class accuracy ranges from 69\% (C1) to 92\% (C8).}
