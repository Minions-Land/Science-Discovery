\caption{Ridgeline plot of log-expression distributions across six clusters. Each ridge shows a kernel density estimate (bandwidth 0.3) for the samples in that cluster, stacked with mild vertical overlap. Clusters are ordered from bottom (Cluster~1, lowest median expression) to top (Cluster~6, highest), revealing a progressive shift in the expression distribution across the cluster hierarchy.}
