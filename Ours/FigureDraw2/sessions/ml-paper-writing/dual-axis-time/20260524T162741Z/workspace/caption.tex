\caption{Training loss (left axis, log scale, blue) and cosine learning-rate schedule (right axis, red dashed) over 200 training steps. Loss decreases monotonically from $\approx\!2.19$ to $0.21$; the learning rate decays from $10^{-3}$ to zero following a cosine annealing schedule. Axis tick colours match their respective lines to aid reading. Note that the two axes measure different quantities (loss and learning rate), so their magnitudes are not directly comparable.}
