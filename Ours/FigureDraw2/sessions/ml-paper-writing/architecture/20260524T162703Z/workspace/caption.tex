\caption{\textbf{RetroDiff system architecture.} Solid arrows denote forward data flow; the dashed arrow from Prior Network back to Diffusion Decoder denotes a control/feedback dependency. Nodes are grouped into two subsystems: \emph{Retrieval} (Retriever and Knowledge Base) and \emph{Generation} (Diffusion Decoder and Prior Network). The pipeline takes a natural-language Query as input and produces a Generated Sample as output.}
