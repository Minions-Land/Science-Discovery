\caption{%
  Accuracy (\%) of four methods---Method-A, Method-B, Method-C, and
  \textbf{RetroDiff} (green, cross-hatch)---across five reasoning benchmarks
  (MMLU, HumanEval, GSM8K, MATH, GPQA).
  Error bars show sample standard deviation over 5 random seeds.
  \textbf{RetroDiff} achieves the highest accuracy on every benchmark,
  with an average gain of $\mathbf{+6.4}$\,pp over the strongest baseline
  (Method-B) and the largest absolute improvement on MATH ($+4.8$\,pp).
  \emph{Note: data are synthetic but follow realistic distributions;
  see \texttt{captions.json} in the workspace.}%
}
