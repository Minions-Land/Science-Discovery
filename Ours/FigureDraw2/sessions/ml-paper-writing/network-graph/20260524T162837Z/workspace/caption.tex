\caption{Force-directed layout of a 30-node network with four communities (colors).
Nodes are arranged using the Fruchterman--Reingold algorithm (spring layout, seed 42).
Edge opacity is reduced to 0.25 to minimize visual clutter.
Node and edge data are drawn directly from \texttt{data.json}; no values were mocked.}
