\caption{Sankey diagram of the dataset preparation pipeline. Starting from 1,\!120 raw samples, 180 are rejected across two quality-filtering stages (Raw\,$\to$\,Reject: 120; Cleaned\,$\to$\,Reject: 60). The remaining 820 samples pass through a cleaning step, yielding 820 cleaned instances; after a second filtering pass (value 820; 180 lost gives 640 total reaching the split), the surviving 820 samples are partitioned into Train (600), Val (120), and Test (100) splits. Node widths and ribbon thicknesses are proportional to sample counts.}
