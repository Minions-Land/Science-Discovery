\caption{\textbf{\methodname}: Retrieval-Augmented Diffusion for Efficient Reasoning.
  The two-page paper section (compiled as \texttt{main.pdf}) presents
  the method architecture (\cref{fig:hero}), benchmark accuracy across five datasets
  (\cref{fig:accuracy}), training efficiency (\cref{fig:loss}), and Chinchilla-style
  scaling behaviour (\cref{fig:scaling}).
  All data are synthetic but drawn from realistic distributions.
  \texttt{figure.pdf} is a copy of \texttt{main.pdf}; the LaTeX source is fully
  reproducible via \texttt{gen\_figure.py}.}
