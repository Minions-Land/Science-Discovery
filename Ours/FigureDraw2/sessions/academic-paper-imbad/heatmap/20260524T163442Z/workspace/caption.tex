\caption{%
  Confusion matrix (counts) for a 10-class classifier.
  Rows correspond to true classes (C1--C10) and columns to predicted classes.
  Diagonal cells (correct predictions, shown in green) are annotated in bold white;
  off-diagonal cells (errors) are shaded with the Blues colormap scaled to the maximum observed count.
  Per-class accuracy ranges from 69\% (C1) to 92\% (C8).
}
