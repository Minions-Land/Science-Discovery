\caption{Ridgeline plot of log-expression distributions for six cell clusters. Each ridge shows a kernel density estimate (Gaussian kernel, bandwidth~$= 0.25$) normalised to unit peak height. Clusters are ordered from bottom (Cluster~1) to top (Cluster~6); distributions shift progressively toward higher expression values in later clusters.}
