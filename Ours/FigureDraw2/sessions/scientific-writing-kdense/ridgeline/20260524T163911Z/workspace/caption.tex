\caption{Ridgeline plot of log-expression distributions for six clusters. Each ridge shows a kernel density estimate (Gaussian kernel, Scott's bandwidth rule) of per-sample log expression values ($n \approx 400$--$500$ per cluster). Clusters are stacked vertically with mild overlap; distributions are coloured by cluster. Cluster~1 exhibits the lowest median expression, while Clusters~5 and~6 are shifted toward higher values, indicating progressive upregulation across clusters.}
