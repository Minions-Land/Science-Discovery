\caption{Confusion matrix for a 10-class classifier (counts). Rows indicate true class; columns indicate predicted class. Diagonal cells (correct predictions, shaded with the \textit{viridis} colormap) are annotated in white; off-diagonal cells (misclassifications, shaded with the \textit{Blues} colormap) are annotated in black or white depending on cell intensity. Per-class accuracy ranges from 69\% (C1) to 92\% (C8).}
