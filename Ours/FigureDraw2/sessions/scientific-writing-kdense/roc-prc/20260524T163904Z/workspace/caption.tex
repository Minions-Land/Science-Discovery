\caption{%
  \textbf{ROC and Precision-Recall curves for four binary classifiers.}
  Left: Receiver Operating Characteristic (ROC) curves; the diagonal dotted line
  denotes random performance.
  Right: Precision-Recall (PRC) curves; the horizontal dotted line marks the
  positive-class prevalence ($\approx0.40$).
  AUROC and Average Precision (AP) values are shown in each legend.
  OursModel achieves the highest performance (AUROC$=0.989$, AP$=0.984$),
  followed by Method-B (AUROC$=0.961$, AP$=0.952$),
  Method-A (AUROC$=0.899$, AP$=0.875$),
  and Baseline (AUROC$=0.866$, AP$=0.811$).
  Curves are computed from raw classifier scores provided in \texttt{data.json}.%
}
