\caption{Force-directed layout of a 30-node network partitioned into four communities (colours). Nodes are positioned by a spring-embedding algorithm (NetworkX, seed 42); edge transparency ($\alpha=0.25$) reduces visual clutter. Cross-community edges reveal the ring-like backbone connecting the four groups.}
