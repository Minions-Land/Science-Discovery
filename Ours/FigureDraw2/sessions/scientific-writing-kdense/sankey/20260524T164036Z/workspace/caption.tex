\caption{Sankey diagram of the dataset preparation pipeline.
Starting from 1{,}120 raw documents, 180 are rejected during cleaning and filtering steps, leaving 820 filtered samples
that are split into training (600), validation (120), and test (100) sets.
Flow widths are proportional to sample counts; grey flows indicate rejected documents.}
