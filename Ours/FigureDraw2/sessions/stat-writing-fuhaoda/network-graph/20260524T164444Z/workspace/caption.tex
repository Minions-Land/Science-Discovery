\caption{Force-directed layout of a 30-node network with 4 communities (nodes colored by community assignment). Edges connect consecutive and cross-community node pairs as specified in \texttt{data.json}. Node positions computed via the Fruchterman--Reingold algorithm (NetworkX spring layout, seed 42); edge opacity tuned to reduce visual clutter.}
