\caption{RetroDiff training objective derivation. Equation~\eqref{eq:elbo} is the standard diffusion ELBO (Jensen's inequality). Equation~\eqref{eq:decomp} decomposes it into a reconstruction term and per-step KL divergences. Equation~\eqref{eq:retro} is RetroDiff's contribution: the objective conditions each sample's ELBO on a retrieved exemplar set $R(x)$, reducing to standard diffusion when $R(x)=\varnothing$.}
