\caption{%
  ROC curves (left) and Precision-Recall curves (right) for four binary classifiers evaluated on held-out score distributions ($n_{\text{pos}}=120$, $n_{\text{neg}}=180$ per method; balanced prevalence $\approx0.40$).
  AUROC and average precision (AP) are computed from the provided positive/negative score lists and annotated in each legend.
  The dashed grey diagonal on the ROC panel denotes chance-level performance; the dashed grey horizontal line on the PRC panel marks the dataset prevalence baseline.
  \textbf{OursModel} achieves the highest AUROC and AP among all compared methods.%
}
