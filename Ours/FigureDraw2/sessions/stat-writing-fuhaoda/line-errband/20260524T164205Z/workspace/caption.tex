\caption{Validation loss curves for three methods over 50 training steps (5 seeds each). Shaded bands show 95\% confidence intervals ($\text{mean} \pm \text{CI}_{1/2}$). The $y$-axis uses a log scale. The double-headed arrow at step~49 quantifies the gap between \textbf{OursModel} and Method-A at the final step; OursModel achieves the lowest validation loss throughout training.}
