\caption{10$\times$10 confusion matrix showing per-class prediction counts. Rows correspond to true classes (C1--C10) and columns to predicted classes. Diagonal cells (correct predictions, highlighted in blue) are annotated with their counts; off-diagonal cells are coloured by a sequential \textit{Blues} colormap scaled to the maximum off-diagonal count. Class C8 achieves the highest accuracy (92 correct), while C1 shows the most confusion with neighbouring classes.}
