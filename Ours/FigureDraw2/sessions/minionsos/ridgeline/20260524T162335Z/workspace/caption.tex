\caption{Ridgeline plot of log-expression distributions for six cell clusters. Each ridge shows a kernel density estimate (Gaussian kernel, bandwidth~$= 0.25$) normalised to unit peak height and offset vertically for legibility. Cluster labels are coloured to match their respective ridges. Data are from \texttt{data.json} ($n \approx 400$ observations per cluster).}
