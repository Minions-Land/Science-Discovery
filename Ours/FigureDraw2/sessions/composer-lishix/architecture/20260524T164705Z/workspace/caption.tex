\caption{\textbf{RetroDiff system architecture.} Data flows left-to-right from an input \emph{Query} through a \emph{Retriever} (which reads from a knowledge base, KB) and into a \emph{Diffusion Decoder}. A \emph{Prior Network} conditions the decoder via a dashed control/feedback arrow. The final output is a \emph{Generated Sample}. Dashed rounded rectangles indicate the \emph{Retrieval} and \emph{Generation} stage groups. All nodes and edges are derived directly from \texttt{data.json}.}
