\caption{Ridgeline (stacked KDE) plot of log-expression distributions for six cell clusters. Each ridge shows a kernel-density estimate (bandwidth = 0.3) of per-sample log-expression values; ridges are stacked with mild vertical overlap to reveal shifts in central tendency and spread across clusters. Cluster 1 has the lowest median expression ($\approx$1.9), while Cluster 6 shows the highest ($\approx$3.4). All data are from the provided \texttt{data.json}.}
