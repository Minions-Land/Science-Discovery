---
name: freeze
description: "Freeze a target — stop evolving it"
---

# /freeze — Freeze Target

Call `evo_freeze_target` with the target ID from `$ARGUMENTS`.

Confirm to user: "Frozen {target_id}. It will no longer receive evolution budget."
