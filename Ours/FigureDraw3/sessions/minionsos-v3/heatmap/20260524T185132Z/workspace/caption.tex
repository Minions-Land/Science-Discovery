\caption{%
  Ten-class confusion matrix showing per-row normalised classification rates (\%).
  \textbf{C8 achieves the highest recall at 92\%.}
  Diagonal cells (annotated with percentage and raw count) are coloured with a dark sequential blue scale indicating recall; off-diagonal cells use a lighter sequential blue scale indicating misclassification rate, with darker shading highlighting larger errors.
  $N = 100$ samples per true class; values are row percentages (counts in parentheses on diagonal).%
}
