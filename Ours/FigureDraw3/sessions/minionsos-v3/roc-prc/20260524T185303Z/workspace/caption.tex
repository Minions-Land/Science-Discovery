\caption{%
  \textbf{ROC and Precision-Recall curves for four binary classifiers.}
  \textbf{(a)} Receiver operating characteristic (ROC) curves; dotted diagonal marks random-chance performance.
  \textbf{(b)} Precision-recall (PRC) curves; dotted horizontal line marks the random-classifier baseline at the dataset prevalence ($\approx0.40$).
  OursModel achieves the highest area under both curves (\textbf{AUROC\,=\,0.930, AP\,=\,0.921}), followed by Method-B, Method-A, and Baseline.
  AUROC and average precision (AP) are reported in each legend entry.
  Curves are computed from raw classifier scores on $n_{\text{pos}}=120$, $n_{\text{neg}}=180$ held-out samples per method.%
}
