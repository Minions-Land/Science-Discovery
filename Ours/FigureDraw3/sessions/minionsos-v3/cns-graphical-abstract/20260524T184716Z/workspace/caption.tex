\caption{\textbf{RetroDiff doubles hit rate over baseline diffusion at fixed compute.} RetroDiff retrieves top-$k$ similar molecules from ChEMBL via ECFP4 fingerprints and feeds them as exemplars to a retrieval-augmented diffusion decoder conditioned on the protein binding pocket, yielding a +18\% binding affinity gain (Vina docking score), 60\% data efficiency (matches baseline final loss in 60\% of training steps), and a confirmed scaling law ($R^{2} > 0.9$, five model sizes).}
