\caption{\textbf{Architecture of RetroDiff.} A query enters the Retrieval module (dashed border), where the Retriever fetches relevant entries from the knowledge base (KB, cylinder). Retrieved context is passed to the Generation module, where the Diffusion Decoder produces the output sample; a Prior Network conditions the decoding process via a feedback connection (dashed arrow). Solid arrows denote forward data flow; dashed arrows denote control or feedback.}
