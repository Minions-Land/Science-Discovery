\caption{Force-directed layout of a 30-node network with four communities (colors). Nodes are arranged by a spring-layout algorithm (NetworkX, seed 42); edge transparency reduces visual clutter. Cross-community edges are visible as inter-color connections. Community assignments are taken directly from \texttt{data.json}.}
