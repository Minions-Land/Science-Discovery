\caption{Ridgeline plot of log-expression distributions across six clusters.
Each ridge shows a kernel-density estimate (Gaussian bandwidth $= 0.25$)
for $n \approx 400$--500 samples per cluster; peaks are normalised to unit height.
Clusters are coloured from blue (Cluster~1, lowest median) to red (Cluster~6,
highest median), reflecting a progressive shift in expression level.}
