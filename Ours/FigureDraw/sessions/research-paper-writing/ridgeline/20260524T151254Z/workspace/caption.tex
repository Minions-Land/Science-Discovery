\caption{Ridgeline plot of log-expression distributions across six cell clusters. Each ridge shows a kernel density estimate (Gaussian kernel, bandwidth $= 0.25$) of per-sample log-expression values; ridges are stacked with mild vertical overlap and coloured from blue (Cluster~1) to red (Cluster~6). Clusters 1--2 exhibit lower, narrower distributions, while Clusters 3--6 show progressively higher median expression and heavier right tails.}
