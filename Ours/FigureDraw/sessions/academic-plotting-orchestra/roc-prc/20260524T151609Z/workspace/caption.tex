\caption{%
  \textbf{ROC and Precision-Recall curves for four binary classifiers.}
  (a)~Receiver Operating Characteristic (ROC) curves; the dashed diagonal marks
  random-chance performance (AUROC\,=\,0.50).
  (b)~Precision-Recall (PR) curves; the dashed horizontal line marks the
  positive-class prevalence baseline.
  AUROC and Average Precision (AP) are annotated in each legend.
  \textit{OursModel} achieves the highest AUROC and AP among all compared methods.
  Curves are computed directly from the provided classifier scores.%
}
