\caption{Ridgeline plot of log-expression distributions across six cell clusters ($n \approx 400$--$500$ per cluster). Each ridge shows a kernel-density estimate (Scott's rule) normalised to unit peak height and offset vertically for legibility. Clusters are ordered from bottom (Cluster~1) to top (Cluster~6); the progressive shift in distribution mode from $\approx$1.9 (Cluster~1) to $\approx$3.4 (Cluster~6) reflects increasing mean expression across the cluster hierarchy.}
