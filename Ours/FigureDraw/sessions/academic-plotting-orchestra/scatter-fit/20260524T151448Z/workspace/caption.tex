\caption{Chinchilla-style neural scaling law. Each point represents a model evaluated at a given parameter count (log\textsubscript{10} scale). The OLS regression line ($y = -0.0449x + 0.621$, $R^2 = 0.877$) confirms a strong negative relationship between model size and validation loss. The highlighted \textbf{OursModel} point (log\textsubscript{10}(params)\,$\approx$\,9.45, loss\,$\approx$\,0.210) is the closest data point to the target coordinates $(9.4,\,0.205)$.}
