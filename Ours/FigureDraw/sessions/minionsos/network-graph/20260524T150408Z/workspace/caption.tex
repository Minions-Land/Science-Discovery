\caption{Force-directed layout of a 30-node network partitioned into four communities (colors). Nodes are arranged using the spring-layout algorithm (NetworkX, seed 42); edges are drawn with reduced opacity to limit visual clutter. The graph structure follows a near-ring topology with additional cross-community shortcut edges, producing moderate inter-community connectivity.}
