\caption{Ridgeline plot of log-expression distributions for six cell clusters. Each ridge shows a kernel density estimate (Gaussian kernel, bandwidth factor 0.25) computed from the provided sample values. Clusters are stacked with mild vertical overlap; distributions shift progressively rightward from Cluster~1 (lowest median) to Cluster~6 (highest median), indicating increasing expression levels across clusters.}
