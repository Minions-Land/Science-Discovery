\caption{%
  \textbf{\textsc{RetroDiff} paper section (3 pages).}
  A NeurIPS-style two-column paper section comprising a title, $\sim$150-word
  abstract, and two sections (Method and Results) that reference four provided
  figures (\texttt{fig01}--\texttt{fig04}).
  The method name and all recurring named entities are macro-driven
  (\texttt{\textbackslash methodname}, \texttt{\textbackslash baselineA},
  \texttt{\textbackslash benchMATH}, etc.).
  Three hard claims are supported: (1)~\textsc{RetroDiff} outperforms strong
  baselines on five reasoning benchmarks, with the largest gain on MATH
  ($+4.8$\,pp); (2)~training is data-efficient, matching the next-best
  method's final loss in 60\% of training steps; (3)~a Chinchilla-style
  scaling law holds with $R^{2}>0.9$.
  \texttt{figure.pdf}/\texttt{figure.png} show page~1 of the compiled paper;
  the full 3-page \texttt{main.pdf} is the primary output.
  No data was mocked; all claims derive from \texttt{claim.json} and
  \texttt{captions.json}.
}
