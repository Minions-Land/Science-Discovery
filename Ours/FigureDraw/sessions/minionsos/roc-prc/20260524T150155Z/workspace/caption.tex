\caption{%
  \textbf{ROC and Precision-Recall curves for four binary classifiers.}
  (a)~Receiver Operating Characteristic (ROC) curves; the dashed diagonal is the random-chance baseline.
  (b)~Precision-Recall (PR) curves; the dashed horizontal line marks the positive-class prevalence ($\approx0.40$).
  Area Under the ROC curve (AUROC) and Average Precision (AP) are annotated in each legend.
  Scores were computed from the raw positive/negative score distributions in \texttt{data.json}.
}
