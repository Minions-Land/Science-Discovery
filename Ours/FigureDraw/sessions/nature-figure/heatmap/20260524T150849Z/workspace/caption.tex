\caption{%
  \textbf{10$\times$10 confusion matrix.}
  Each cell $(i,j)$ shows the number of samples from true class $C_i$
  predicted as class $C_j$ (raw counts).
  Diagonal cells (correct predictions) are rendered in deep purple
  with white annotation for contrast; off-diagonal cells use a
  sequential \textit{Blues} colormap scaled to the maximum off-diagonal
  count, revealing the magnitude of misclassifications.
  Per-class accuracy ranges from 69\,\% (C1) to 92\,\% (C8).%
}
