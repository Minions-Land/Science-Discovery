\caption{Ridgeline plot of log-expression distributions across six single-cell clusters.
Each ridge shows a kernel density estimate (Gaussian kernel, Scott bandwidth) for the
indicated cluster ($n \approx 400$–$500$ cells per cluster). Clusters are ordered from
bottom (Cluster~1) to top (Cluster~6) with mild vertical overlap to facilitate visual
comparison of distribution shape, centre, and spread. Colours progress from deep blue
(Cluster~1) to red (Cluster~6). All data are from \texttt{data.json}.}
