\caption{%
  \textbf{ROC and Precision--Recall curves for four binary classifiers.}
  (\textit{Left}) Receiver operating characteristic (ROC) curves; the grey dashed diagonal
  indicates chance performance.
  (\textit{Right}) Precision--Recall (PRC) curves; the grey dashed horizontal line marks the
  mean class prevalence ($\approx0.44$) as the no-skill baseline.
  AUROC / average precision (AP) are annotated per method in the legend.
  OursModel achieves the highest discrimination (AUROC\,=\,0.989, AP\,=\,0.984),
  followed by Method-B (AUROC\,=\,0.961, AP\,=\,0.952),
  Method-A (AUROC\,=\,0.899, AP\,=\,0.875),
  and Baseline (AUROC\,=\,0.866, AP\,=\,0.811).
  Curves are computed directly from the score distributions provided in \texttt{data.json};
  no data were mocked or imputed.%
}
