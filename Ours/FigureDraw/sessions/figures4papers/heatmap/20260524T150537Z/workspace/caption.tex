\caption{Confusion matrix for a 10-class classifier (classes C1--C10). Rows correspond to true labels and columns to predicted labels. Diagonal cells (annotated in bold white on a \texttt{YlOrBr} scale) show per-class correct-prediction counts; off-diagonal cells are coloured by a \texttt{Blues} sequential colormap to highlight the most common misclassifications. Class~C8 achieves the highest per-class accuracy (92 correct), while C1 has the lowest (69 correct).}
