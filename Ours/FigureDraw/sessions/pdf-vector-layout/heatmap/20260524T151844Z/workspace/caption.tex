\caption{10$\times$10 confusion matrix showing prediction counts for classes C1--C10. Rows correspond to true classes; columns to predicted classes. Diagonal cells (correct predictions) are shaded with the \texttt{viridis} colormap and annotated in white; off-diagonal cells (misclassifications) use a sequential \texttt{Blues} colormap scaled to the maximum off-diagonal count.}
