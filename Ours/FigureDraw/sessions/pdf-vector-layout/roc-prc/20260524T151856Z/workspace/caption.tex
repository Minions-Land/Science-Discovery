\caption{%
  ROC curves (left) and Precision--Recall curves (right) for four binary classifiers computed from
  raw logit scores in \texttt{data.json} (120 positive / 180 negative examples per method).
  Area under the ROC curve (AUC) and average precision (AP) are annotated in each legend.
  The dashed line on the ROC panel is the random-chance diagonal; the dashed line on the
  Precision--Recall panel marks the prevalence baseline (AP\,$=$\,0.500 at balanced class ratio).
  \textbf{OursModel} achieves the highest AUC and AP across all thresholds.
}
