\caption{Confusion matrix for a 10-class classifier (C1--C10). Rows correspond to true classes and columns to predicted classes. Diagonal cells (correct predictions, shown in green) are annotated in bold white; off-diagonal cells (misclassifications) are shaded by count using a sequential Blues colormap. Diagonal counts range from 69 (C1) to 92 (C8), indicating per-class accuracy between 69\% and 92\% assuming 100 samples per class.}
