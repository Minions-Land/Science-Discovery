\caption{\textbf{RetroDiff system architecture.} A query enters the \textit{Retrieval} module, where the Retriever fetches relevant documents from the knowledge base (KB) via a bidirectional lookup (solid double-headed arrow). The retrieved context is passed to the \textit{Generation} module, where the Diffusion Decoder produces the output sample. A dashed arrow from the Diffusion Decoder to the Prior Network denotes a control/conditioning signal (feedback). Solid arrows indicate data flow; dashed arrows indicate control or conditioning.}
