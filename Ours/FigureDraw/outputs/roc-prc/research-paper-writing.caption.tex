\caption{%
  ROC curves (left) and Precision--Recall curves (right) for four binary
  classifiers computed from held-out scores in \texttt{data.json}.
  The dashed diagonal in the ROC panel denotes random-chance performance;
  the dashed horizontal line in the PR panel marks the positive-class
  prevalence ($p=0.40$).
  Legend entries report area under the ROC curve (AUROC) and
  average precision (AP) for each method.
  \textsf{OursModel} achieves the highest AUROC and AP.
}
