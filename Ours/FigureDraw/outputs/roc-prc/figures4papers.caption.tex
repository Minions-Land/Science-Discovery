\caption{%
  ROC (left) and Precision-Recall (right) curves for four binary classifiers computed from raw
  score distributions in \texttt{data.json} ($n_{\text{pos}}\approx120$, $n_{\text{neg}}\approx180$
  per method).
  AUROC and average precision (AP) are annotated in each legend.
  The diagonal dotted line in the ROC panel marks the chance classifier;
  the horizontal dotted line in the PRC panel marks the positive-class prevalence ($\approx0.43$).
  \textbf{OursModel} achieves the highest AUROC and AP among all compared methods.%
}
